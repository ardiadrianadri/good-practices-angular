/**
 * Define the required attributes for the table component
 *
 * @export
 * @interface BaseElement
 */
export interface BaseElement {
  /**
   * Id of the object
   *
   * @type {string}
   * @memberof BaseElement
   */
  id: string;
}
